# WSL 122B Test Notes - 2026-05-19

## Environment

- WSL ext4 repo: `/home/jiamin/projects/CFIE`
- 122B model: `/home/jiamin/models/Qwen3.5-122B-A10B-GPTQ-Int4/snapshots/5b9f0050d3ec98b0c81a7716776533c5eacebb64`
- Expert cache: `.cfie_marlin_ready_cache/v1/`
- GPU: RTX 5090 32GB
- Storage rule for future tests:
  - `D:` is the PCIe 5 high-speed drive and is faster than the system `C:` drive for model/cache experiments.
  - WSL ext4 VHDX lives under `D:\WSL\Ubuntu-22.04-D`; WSL reports the root filesystem as `/dev/sdd`, about 1.0 TB.
  - Windows-side test model/cache should live under `D:\models\...`; avoid benchmarking from the default `C:\Users\13642\.cache\huggingface\...` path unless the goal is explicitly a C-vs-D startup comparison.
  - Non-graph experiments should prefer Windows on the D-drive model/cache. Keep WSL mainly for Linux-only PIECE cudagraph validation.

## Completed

- Synced Windows repo to WSL ext4.
- `prefill_burst_slots=256` burst target now reuses the runtime-ready batch write path.
- Burst mode disables split-MoE to avoid holding the shared burst pool across PIECE prepare/compute boundaries.
- `cfie::moe_forward` and `cfie::moe_forward_shared` are added as burst-mode PIECE splitting ops.
- WSL unit test: `tests/unit/test_moe_tiered_cache.py`, 78 passed.
- Runtime Marlin-ready expert cache no longer has a `.pt` load path in `cfie/offload/weight_offload.py`; runtime cache files are safetensors-only.
- Cache conversion status on 2026-05-20:
  - WSL ext4 model cache: 96 `.safetensors` files, 0 `.pt` files, about 111.80 GiB total. This includes default/A16 and `fp8` activation cache directories.
  - Windows `C:` HuggingFace cache: 48 `.safetensors` files, 0 `.pt` files, about 55.90 GiB total. Current contents are the `fp8` cache directory.
  - Windows `D:` test cache: 96 `.safetensors` files, 0 `.pt` files, about 111.80 GiB total. This includes default/A16 and `fp8` activation cache directories.

## Key Findings

- `cudagraph_capture_sizes=500` is wrong for decode-only testing. It pads single-token decode to a 500-token shape, makes graph-external MoE see too many routed experts, and forces 256-slot burst work. Result: about 0.69 token/s.
- `cudagraph_capture_sizes=1 16` gives faster numbers, about 5.50 total token/s and 8.97 steady token/s, but current output text is incorrect. It is not a production-safe default.
- Correct output currently requires eager mode.
- `cudagraph_copy_inputs=1` currently fails during capture with a stride assertion, so it cannot be used as a quick fix for graph-external MoE output buffers.

## Correct-Output 512 Decode

| gpu_slots_per_layer | graph | output | total TPS | steady TPS | first step |
| --- | --- | --- | ---: | ---: | ---: |
| 16 | off / eager | ok | 4.82 | 5.36 | 10.97s |
| 24 | off / eager | ok | 5.03 | 5.63 | 10.94s |
| 32 | off / eager | ok | 5.05 | 5.69 | 11.54s |
| 16 | PIECE, sizes=1 16 | wrong | 5.50 | 8.97 | 36.16s |
| 16 | PIECE, size=500 | wrong/unusable | 0.69 | 0.72 | 35.06s |

## Memory And Startup

- `gpu_slots_per_layer=16` eager: model loading 16.25 GiB, about 341s.
- `gpu_slots_per_layer=24` eager: model loading 18.01 GiB, about 326s.
- `gpu_slots_per_layer=32` eager: model loading 19.75 GiB, about 340s.
- PIECE graph `sizes=1 16`: graph capture about 78s, extra 3.71 GiB.
- Pinned static layers: 0-37 pinned, 38-47 pageable.
- After the CPU `_expert_to_slot` mirror optimization, WSL decode512 with safetensors cache, `gpu_slots_per_layer=16`, `prefill_burst_slots=256`, decode graph sizes `1 2`, and fp16/a16 Marlin input produced:
  - total: 512 tokens in 109.55s, 4.67 token/s
  - steady decode after first step: 5.99 token/s
  - first step: 24.20s
- Best earlier correct-output WSL decode512 before the safetensors-only runtime switch was:
  - total: 512 tokens in 101.75s, 5.03 token/s
  - steady decode after first step: 6.48 token/s
  - first step: 22.93s
- Current safetensors startup is much faster than old `.pt` expert cache attach:
  - safetensors startup smoke: dense weights about 47.3s, expert cache attach about 136s, model loading about 189.8s
  - older `.pt`/legacy attach path: expert attach about 242s, model loading about 298s
- Windows+D non-graph decode512 on 2026-05-20:
  - command requested `--marlin-input-dtype fp8`, but the Windows GPTQ Marlin MoE path logged that fp8 activation input was disabled; treat this as an A16/default-path performance sample, not a valid W4A8 result.
  - `gpu_slots_per_layer=32`, `prefill_burst_slots=256`, `cpu_static_pinned_gb=56`, `prepare_cpu_copy_batch_size=8`, `kv_cache_memory_bytes=1GiB`, eager/no graph.
  - total: 512 tokens in 85.59s, 5.98 token/s
  - steady decode after first step: 6.02 token/s
  - first measured decode step: 0.75s
  - startup wall clock: 12:49:22 to 12:58:07, about 524.5s; dense weights 40.3s, expert attach about 330.0s, model loading total about 375.5s
  - static expert mirror: 44.25 GiB pinned across 38 layers, 11.65 GiB pageable across 10 layers
  - resident hit summary from prepare logs: avg hit 63.9%, avg missing 2.89 experts/layer; layers 32-47 averaged 71.2% hit and 2.31 missing experts/layer
- Windows+D cache-hit smoke on 2026-05-20 after default/A16 cache was complete:
  - `gpu_slots_per_layer=32`, `prefill_burst_slots=256`, `cpu_static_pinned_gb=56`, `prepare_cpu_copy_batch_size=8`, `kv_cache_memory_bytes=1GiB`, `marlin_input_dtype=auto`, eager/no graph.
  - wall clock: 13:02:09 to 13:05:09, about 180s
  - dense weights: 32.0s
  - expert cache attach: 111.0s
  - model loading total: 147.7s, GPU memory 19.75 GiB
  - static expert mirror: 44.25 GiB pinned across 38 layers, 11.65 GiB pageable across 10 layers
  - short decode: 16 tokens in 3.18s, 5.04 token/s total, 5.91 token/s steady
  - generated prefix started with a coherent Qwen reasoning block (`<think>`); no repetitive Zone-list corruption was observed in this smoke.
- Windows+D cache-hit smoke with `cpu_static_pinned_gb=44`:
  - wall clock: 13:06:37 to 13:09:17, about 160.9s
  - dense weights: 38.9s
  - expert cache attach: 86.0s
  - model loading total: 129.7s, GPU memory 19.75 GiB
  - static expert mirror: 43.09 GiB pinned across 37 layers, 12.81 GiB pageable across 11 layers
  - short decode: 16 tokens in 3.24s, 4.94 token/s total, 6.53 token/s steady
  - no pinned allocation retry/fallback noise was observed; this is the current preferred startup setting over `cpu_static_pinned_gb=56`.
- Windows+D current 512-token correctness baseline with `cpu_static_pinned_gb=44`:
  - `gpu_slots_per_layer=32`, `prefill_burst_slots=256`, `prepare_cpu_copy_batch_size=8`, `kv_cache_memory_bytes=1GiB`, `marlin_input_dtype=auto`, eager/no graph.
  - wall clock: 13:10:29 to 13:14:38, about 248.9s
  - dense weights: 38.7s
  - expert cache attach: 83.0s
  - model loading total: 126.8s, GPU memory 19.75 GiB
  - decode: 512 tokens in 90.32s, 5.67 token/s total, 5.72 token/s steady
  - static expert mirror: 43.09 GiB pinned across 37 layers, 12.81 GiB pageable across 11 layers
  - prepare stats: avg resident hit 55.3%, avg missing 3.58 experts/layer; layers 00-15 were the worst at 30.3% hit and 5.58 missing experts/layer.
  - prompt was passed through PowerShell and Chinese text appeared mojibake in the saved prefix; use ASCII prompts or file-based UTF-8 prompts for future Windows text-quality checks.
- Windows+D timing run with `CFIE_BENCH_TIMING=1`, 64 tokens:
  - timing mode inserts CUDA synchronizations, so use it for bottleneck attribution only, not final TPS.
  - steady timing speed: 4.84 token/s.
  - startup: dense weights 38.8s, expert cache attach 81.0s, model loading total 124.8s.
  - prepare stats: avg resident hit 60.0%, avg missing 3.35 experts/layer; layers 00-15 averaged 39.6% hit and 4.98 missing experts/layer.
  - steady prepare per generated token: plan 22.1ms, stage_write 41.6ms, stage_cpu_pack 8.7ms, stage_h2d 7.0ms, stage_gpu_scatter 5.2ms, stage_install 4.3ms, total 74.2ms.
  - steady MoE runner per generated token: prepare_sync 87.9ms, compute_sync 14.7ms, apply_sync_total 102.5ms.
  - Current bottleneck is the synchronized prepare path as a whole; raw pageable/pinned CPU pack is not the dominant subcomponent in this run.
- Windows+D forced MoE W4A8 smoke on 2026-05-20:
  - old run used a temporary fp8 env gate plus `--marlin-input-dtype fp8`; the env gate has since been removed and the CLI now controls Marlin activation dtype directly.
  - one stale fp8 cache file for layer 39 was rebuilt during startup.
  - startup: dense weights 39.1s, expert cache attach 92.0s, model loading total 136.7s.
  - speed: 64 tokens in 9.65s, 6.63 token/s total, 7.11 token/s steady.
  - output was invalid: repeated `1.` lines. This run is superseded by the fp8 offload-cache fix below.
- MoE W4A8 failure investigation on 2026-05-20:
  - Activation scale metadata is dynamic per-token at runtime via `marlin_quant_input(...)`; it is not a missing static weight-cache field.
  - A local one-layer WSL check loaded `layer_000.safetensors` from both A16 and `fp8` Marlin-ready caches and ran identical random hidden/topk through `fused_marlin_moe`.
  - Existing `fp8` cache produced near-zero MoE output: fp8 std `3.84e-12` vs A16 std `1.40e-4`.
  - The fp8 cache qweights differ from A16 as expected, but fp8 scales did not carry the required `*512` compensation; this matches the near-zero output and the repeated-token full-model failure.
  - Code now rejects old fp8 Marlin-ready expert caches unless metadata includes `fp8_weight_scale_factor=512`. A16 cache remains valid.
- MoE W4A8 offload-cache fix on 2026-05-20:
  - Root cause: the offload raw GPTQ -> runtime cache path did not mirror `GPTQMarlinMoEMethod.process_weights_after_loading(...)` for fp8 activation. It skipped `marlin_int4_fp8_preprocess(...)` on qweights and skipped the `*512` weight-scale compensation before Marlin scale permutation.
  - Fix: `LayerTieredExpertCacheController` now applies the fp8 qweight preprocess and scale compensation in both CPU static cache construction and the remaining dynamic write fallback. A new fp8-only metadata field `fp8_preprocess_schema_version=2` invalidates stale fp8 caches that already had `fp8_weight_scale_factor=512` but were built by the broken order.
  - WSL one-layer check after the fix: old fp8 cache output was all zero; newly generated fp8 runtime output matched A16 with mean relative error about `4.5%`. The A16 raw path still exactly matched the existing A16 cache.
  - Validation: WSL `py_compile cfie/offload/weight_offload.py` passed; WSL `pytest tests/unit/test_moe_tiered_cache.py -q` passed (`78 passed`). Windows `py_compile` passed. Windows unit tests still have unrelated policy-budget failures in `test_moe_tiered_cache.py`.
  - Windows+D W4A8 cache rebuild smoke: `gpu_slots_per_layer=32`, `prefill_burst_slots=256`, `cpu_static_pinned_gb=44`, `prepare_cpu_copy_batch_size=8`, `kv_cache_memory_bytes=1GiB`, `--enforce-eager`, `--marlin-input-dtype fp8`.
  - Rebuilding all fp8 layer caches took model loading to 339.4s. The generated text was coherent and started with `<think>` plus a GPU inference checklist; no repeated `1.` corruption.
  - W4A8 short smoke: 16 tokens in 3.93s, 4.08 token/s total, 5.99 token/s steady, first step 1.42s.
  - W4A8 cache-hit decode512: model loading 118.8s, 512 tokens in 94.93s, 5.39 token/s total, 5.46 token/s steady, first step 1.38s.
  - Same-code A16/auto cache-hit decode512 control: model loading 126.0s, 512 tokens in 95.45s, 5.36 token/s total, 5.43 token/s steady, first step 1.41s.
  - Conclusion: W4A8 correctness is fixed for this decode512 smoke and is roughly tied with A16/auto in end-to-end decode because prepare/offload still dominates. Treat W4A8 as usable but not yet a decisive speed win until more prompts and graph/MTP tests are repeated.
- Direct pinned expert-major storage experiment:
  - changed safetensors cache load so pinned CPU static layers first try to pin the whole expert-major `expert_storage` tensor directly, then build per-expert views from that storage.
  - validation: `py_compile cfie/offload/weight_offload.py` passed; `tests/unit/test_moe_tiered_cache.py` passed, 78 tests.
  - Windows+D smoke result: wall clock about 161.9s, dense weights 38.9s, expert attach 84.0s, model loading total 127.9s.
  - This is only a small improvement versus the earlier `cpu_static_pinned_gb=44` cache-hit run at 129.7s model loading. The remaining startup cost is not dominated by Python field-pack alone; it is the serial safetensors load plus whole-layer CPU copy/pin plus CPU mirror registration path.
- WSL decode-only PIECE graph and MTP smoke on 2026-05-20:
  - Env gates were removed for graph/fp8 control. `--marlin-input-dtype fp8` now writes `CompilationConfig.marlin_input_dtype`; `--allow-tiered-moe-compile` controls the tiered-MoE graph gate.
  - `--cudagraph-prefill-capture-sizes` now accepts an empty list, so decode graph can use `[1, 2]` while prefill capture is disabled.
  - Tiered MoE boundary ops are always registered for PIECE graph when tiered cache is enabled: `cfie::moe_prepare_tiered`, `cfie::moe_forward`, `cfie::moe_forward_shared`.
  - `gpu_slots_per_layer=16`, `prefill_burst_slots=16` failed even on the short prompt: one prefill layer required 36 unique experts, exceeding resident 16 + burst 16. This means `burst_16` is not a valid long-prefill configuration without a new chunking/burst policy.
  - Working graph baseline: `gpu_slots_per_layer=16`, `prefill_burst_slots=256`, `cpu_static_pinned_gb=44`, `prepare_cpu_copy_batch_size=8`, `kv_cache_memory_bytes=1GiB`, `--marlin-input-dtype fp8`, decode graph sizes `[1,2]`, prefill graph sizes `[]`.
  - Non-MTP 64 decode under this CLI/config path: model load 194.9s, graph capture 74s / 3.70GiB, engine init 81.7s, 64 tokens in 64.97s, total 0.985 token/s, steady 1.98 token/s, first step 33.12s.
  - MTP=1 initially failed in graph key setup because drafter decode graph tried to register size 1 with `uniform_decode_query_len=2`. Fix: dispatcher now initializes from its own speculative query len and filters decode sizes to multiples of that len.
  - MTP=1 then failed because the draft MoE workspace tried to grow after graph lock: required 0.47MiB, current 0.19MiB. Fix: speculative graph capture reserves 16MiB workspace before lock.
  - MTP=1 after both fixes completed: model load 315.5s, graph capture 34s / 3.66GiB, engine init 41.6s, 64 tokens in 48.24s, total 1.33 token/s, steady 1.94 token/s, first step 15.81s, 34 engine steps.
  - Correction: these 64-token graph numbers are not a valid performance baseline. They are graph/MTP smoke tests only. A follow-up non-MTP run with `max_num_batched_tokens=512`, `--cudagraph-copy-inputs`, the same `gpu_slots_per_layer=16`, `prefill_burst_slots=256`, `cpu_static_pinned_gb=44`, and `--marlin-input-dtype fp8` still measured only 64 tokens in 71.75s, total 0.892 token/s, steady 1.70 token/s. This indicates the current `fp8 + PIECE graph + graph-external tiered MoE` path is taking a slow path and must not replace the no-graph baseline yet.
  - Current trusted speed baseline remains the Windows+D no-graph decode512 run above: W4A8 total 5.39 token/s, steady 5.46 token/s; A16/auto total 5.36 token/s, steady 5.43 token/s.
- Regression clarification after the graph/MTP smoke:
  - The low `~2 token/s` figure is not acceptable as a default and must not be reported as a successful graph/MTP speed result.
  - `benchmarks/run_decode_512.py` now supports `--prompt-file` and reads it with `utf-8-sig`; this avoids PowerShell/WSL quoting and BOM pollution during cross-platform comparisons.
  - Same prompt and same greedy sampling were retested: `Write a concise checklist for testing GPU inference.`, `temperature=0`, `max_new_tokens=512`, `gpu_slots_per_layer=32`, `prefill_burst_slots=256`, `cpu_static_pinned_gb=44`, `prepare_cpu_copy_batch_size=8`, `kv_cache_memory_bytes=1GiB`, eager/no graph.
  - Windows+D W4A8/fp8 result: 512 tokens in 90.13s, total 5.68 token/s, steady 5.75 token/s, first step 1.33s. This confirms the Windows no-graph path is still healthy under the stricter prompt-file/greedy口径.
  - WSL same-code `marlin_input_dtype=fp8`, `gpu_slots_per_layer=32`: 512 tokens in 210.22s, total 2.44 token/s, steady 2.65 token/s, first step 17.44s.
  - WSL same-code `marlin_input_dtype=auto`, `gpu_slots_per_layer=32`: 512 tokens in 208.67s, total 2.45 token/s, steady 2.66 token/s, first step 16.74s. This rules out W4A8 as the main reason for the WSL slowdown.
  - WSL `gpu_slots_per_layer=64`, `marlin_input_dtype=auto`: model memory rose to 26.55 GiB and decode512 reached only total 2.65 token/s, steady 2.93 token/s, first step 18.64s. Cache hit improved, but WSL remained far below Windows; increasing slots alone is not enough.
  - Model files, tokenizer/config/index files, key Python sources, and sampled expert cache payloads match between Windows+D and WSL. Full-file fp8 cache hashes differ because safetensors metadata embeds different model paths; layer 0 payload hash excluding the header is identical.
  - Current interpretation: the WSL/Linux path is still a separate performance bug/regression path. It shows much slower cold first step and weaker end-to-end decode even with identical prompt/model/cache payload. PIECE graph should remain blocked as a performance feature until WSL no-graph returns to the Windows-level baseline or the graph path proves a real improvement over that WSL baseline.
  - Future speed reports must include decode length and tail-window TPS. 64-token smoke tests are for correctness/launch validation only, not for final token/s claims.

## Current Recommended Correct-Output Default

```bash
--gpu-slots-per-layer 32
--prefill-burst-slots 256
--prepare-cpu-copy-batch-size 8
--cpu-static-pinned-gb 44
--marlin-input-dtype fp8
--enforce-eager
```

Use this as the current Windows+D correct-output speed baseline at `max_model_len=4096`. After the fp8 cache fix, measured steady speed was 5.46-5.75 token/s for W4A8/fp8 and 5.43 token/s for A16/auto under the same command shape. Use `--gpu-slots-per-layer 16` only when memory must be conserved. Keep PIECE graph experimental until output correctness and performance are both fixed.

For WSL decode graph experiments, use explicit CLI/config rather than env gates:

```bash
--gpu-slots-per-layer 16
--prefill-burst-slots 256
--prepare-cpu-copy-batch-size 8
--cpu-static-pinned-gb 44
--kv-cache-memory-bytes 1073741824
--marlin-input-dtype fp8
--piecewise-cudagraph
--cudagraph-decode-capture-sizes 1 2
--cudagraph-prefill-capture-sizes
```

For WSL decode graph experiments only, the current experimental command shape is:

```bash
GPU_SLOTS_PER_LAYER=16
PREFILL_BURST_SLOTS=256
MARLIN_INPUT_DTYPE=auto
PIECEWISE_CUDAGRAPH=1
CUDAGRAPH_COPY_INPUTS=1
DECODE_CAPTURE_SIZES="1 2"
PREFILL_CAPTURE_SIZES=""
CPU_STATIC_PINNED_GB=0
CPU_STATIC_PINNED_LAYERS="0-24,26,31-33,36,38-42,45,47"
```

The explicit pinned-layer set is a pending experiment: it pins high-miss tail layers instead of simply pinning contiguous layers 0-36.

## Next Blockers

- Fix PIECE graph plus graph-external MoE correctness. Candidate paths: stable output buffers for graph-external MoE, or a redesigned split-MoE path that does not keep the shared burst pool busy across layers.
- Capture sizes must separate decode and prefill needs. Decode must include size 1; large prefill sizes such as 500/5000/50000 cannot replace decode shape 1.
- 5w-token prefill is not measured yet; run it only after graph correctness and memory budget are clear.
