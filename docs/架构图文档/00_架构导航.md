# 架构图文档导航

## 1. 目录用途

`架构图文档/` 用来回答：

- 当前代码链路是怎么串起来的。
- 关键入口类和关键调用关系在哪里。
- 哪些能力已经真实接入，哪些还只是规划或兼容路径。

## 2. 子目录

1. `01_训练主线一_训练基座/`
2. `02_训练主线二_Predictor路由MoE/`
3. `03_推理主线一_客户端推理主链/`
4. `04_推理主线二_Predictor隔离接入/`

## 3. Predictor 阅读建议

- 先看 `../路线文档/05_Predictor工程化落地追踪.md`
- 再看 `../路线文档/02_训练主线二_Predictor路由MoE.md`
- 然后进入 `02_训练主线二_Predictor路由MoE/`
- 再看 `../路线文档/04_推理主线二_Predictor隔离接入.md`
- 最后进入 `04_推理主线二_Predictor隔离接入/`

## 4. 当前口径

- Predictor 训练主线已经切到真实 hidden state 驱动。
- Predictor 推理主线已经接入 masked candidate routing。
- shared GPU candidate slots 仍未闭环，阅读架构图时应把它视为“待落地项”，不是“已完成项”。
